# examples/ — statut

Dossier réservé, intentionnellement vide à la date de ce dossier (2026-08-07).

Les exemples JSON concrets pour les 18 contrats cibles sont fournis **directement dans `contracts/*.md`** (un exemple par contrat, inline) plutôt que dupliqués ici sous forme de fichiers `.json` séparés — voir `contracts/README.md`.

Ce dossier reste disponible pour accueillir, au fil de l'implémentation, des jeux d'exemples plus riches (ex. plusieurs variantes par contrat, fixtures de test partagées entre plusieurs tickets) si le besoin s'en fait sentir — voir `17-EXECUTABLE-BACKLOG.md` pour les fixtures déjà spécifiées au niveau ticket (ex. Ticket -1.2).
