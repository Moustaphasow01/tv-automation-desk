# LOT 013 — Front VNext Certification / Backend Contract Readiness

Date: 2026-08-14
Worktree: `/mnt/c/Users/CES/Desktop/TV_Automation_PREPROD`
Branch: `codex/preprod-v4-local-parity-cleanup`
HEAD: `e18b48a310085679c94639420ca0b0b8c78ee70f`

## 1. Scope

Lot 013 certifies the backend/BFF contract readiness required by Front VNext. This lot did not redesign UI/UX and did not enable LIVE or AUTO execution.

Official operational policy preserved:

- `LIVE = OFF`
- `AUTO EXECUTION = OFF`
- `environment = PAPER` does not imply `autoExecutionEnabled = true`
- Front, AI, Telegram and Jarvis cannot call broker/provider APIs directly
- Human confirmation is not a fill
- Provider ACK is not a fill

## 2. Git baseline and tracking risk

Commands executed:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

Result:

```text
branch: codex/preprod-v4-local-parity-cleanup
HEAD: e18b48a310085679c94639420ca0b0b8c78ee70f
git status entries: 638
tracked modified: 142
untracked: 496
```

Classification:

| Class | Evidence | Status |
| --- | --- | --- |
| TRACKED | 142 modified tracked files across backend, docs, deploy, front and packages | PRESENT |
| UNTRACKED_REQUIRED | prior lot source, tests, migrations, reports and quality scripts are visible under `mcp_gpt_desk/`, `packages/`, `infra/postgres/init/`, `scripts/quality/`, `reports/compliance-closure/` | MUST BE INCLUDED IN RELEASE/COMMIT |
| GENERATED / LOCAL_ONLY | local Docker runtime state and `.local/security/supply-chain-sbom.json` generated by `guard:security-supply-chain` | DO NOT TREAT AS PRODUCT SOURCE WITHOUT REVIEW |
| IGNORED | not exhaustively audited in this lot | PARTIAL |
| SECRET_RISK | env examples, deployment templates and provider-related scripts exist in the tree; no secret value was committed by this lot | RELEASE HYGIENE REVIEW REQUIRED |

No destructive cleanup was performed. No `git reset --hard`, no `git clean -fd`.

## 3. Lot 012 no-regression

Lot 012 guarantees preserved:

- notification operator is not broker execution;
- Human Gate remains mandatory before provider command;
- confirmation is idempotent;
- rejection and expiry fail closed;
- stale signal does not create an OrderIntent;
- post-risk quantity/account/side/instrument/entry/stop/targets remain immutable;
- theoretical/manual/broker fills remain distinct.

Regression proof:

- `mcp_gpt_desk/test/semi_manual_paper_readiness.test.js`
- `mcp_gpt_desk/test/portfolio_order_intent_human_gate_lifecycle.test.js`
- `mcp_gpt_desk/test/portfolio_order_intent_execution_service.test.js`
- Full MCP suite: `1082 pass / 0 fail`

## 4. TD2-412 no-regression

Read-only auth no longer fabricates an operator-like identity.

Proof files:

- `mcp_gpt_desk/src/front-control-plane-auth.js:3`
- `mcp_gpt_desk/src/front-control-plane-auth.js:48`
- `mcp_gpt_desk/src/front-control-plane-auth.js:97`
- `mcp_gpt_desk/test/front_control_plane_api.test.js:249`

Runtime proof after API rebuild:

```text
GET /front-api/v1/views/auth-session
status: 200
authenticated: false
sessionState: READ_ONLY
principal.userId: anonymous-read-only
principal.maskedEmail: ""
principal.roles: []
principal.accountScopes: []
session.sessionId: ""
session.browserMaterialExposure: NONE
PAPER.writeEnabled: false
PAPER.tradingEnabled: false
LIVE.status: LOCKED
```

Status: `CONFIRMED NO REGRESSION`.

## 5. Contract Readiness Matrix

| Contract | Backend actual | Front expected | Status | Proof | Remaining |
| --- | --- | --- | --- | --- | --- |
| StrategySignal | Persisted signal lineage exists and is exposed through OrderIntent authority when known | stable IDs and source lineage | PARTIAL | `mcp_gpt_desk/src/front-control-plane-api.js:1328`, `mcp_gpt_desk/test/front_control_plane_api.test.js:539` | full signal detail contract is not yet a dedicated VNext schema |
| ContextGateDecision | BFF can expose context gate decision/reason/version from backend lineage | `TAKE / TAKE_REDUCED / WAIT / REJECT` style decision data | PARTIAL | `apps/desk-control-plane/src/features/order-intent/mapper.ts:48` | runtime real Context Gate decision attached to a live order is not proven in DB |
| PortfolioDecision | BFF exposes portfolio arbitration stage when lineage contains it | no front-invented allocation status | PARTIAL | `apps/desk-control-plane/src/features/order-intent/mapper.ts:49` | no dedicated portfolio decision schema sample in runtime proof |
| GlobalRiskDecision | BFF exposes backend risk decision, reason codes and policy version from canonical lineage | risk is backend-owned, not recalculated by front | FAIT | `mcp_gpt_desk/src/broker-execution-repository.js:73`, `apps/desk-control-plane/src/features/order-intent/mapper.ts:50`, `mcp_gpt_desk/test/front_control_plane_api.test.js:613` | none for BFF readiness |
| TargetPosition | BFF exposes canonical target position ID/account/authorizedQuantity | read-only post-risk target | FAIT | `mcp_gpt_desk/src/broker-execution-repository.js:73`, `mcp_gpt_desk/test/front_control_plane_api.test.js:614` | none for BFF readiness |
| OrderIntent | BFF resolves post-risk Portfolio OrderIntent even before a broker order exists | `orderIntentId`, terms, revision, immutable source | FAIT | `mcp_gpt_desk/src/front-control-plane-api.js:1249`, `mcp_gpt_desk/src/front-control-plane-api.js:1298`, `mcp_gpt_desk/test/front_control_plane_api.test.js:539` | runtime DB has 4 lineage rows but no active provider command/fill rows |
| HumanExecutionGate | BFF publishes gate status/actions and commands route through backend broker service | confirm/reject from backend allowedActions only | FAIT | `mcp_gpt_desk/src/front-control-plane-api.js:1381`, `mcp_gpt_desk/src/front-control-plane-command.js:16`, `mcp_gpt_desk/src/front-control-plane-command.js:151`, `mcp_gpt_desk/test/front_control_plane_api.test.js:434` | real operator-session confirm is not runtime-proven without operator credentials |
| executionMode | BFF exposes `SEMI_MANUAL` on projected OrderIntent | front displays backend authority | FAIT | `mcp_gpt_desk/src/front-control-plane-api.js:1269`, `mcp_gpt_desk/test/front_control_plane_api.test.js:615` | none for BFF readiness |
| autoExecutionEnabled | Policy preserved as disabled; commands expose `brokerExecution: false` | front must not infer AUTO from PAPER | FAIT | `mcp_gpt_desk/src/front-control-plane-command.js:16`, `mcp_gpt_desk/src/front-control-plane-command.js:173`, runtime `/front-api/v1/capabilities` | no AUTO activation authorized |
| allowedActions | Backend command catalog publishes actions; VNext consumes `humanGate.actions` | no status-to-action inference | FAIT | `mcp_gpt_desk/src/front-control-plane-command.js:3`, `mcp_gpt_desk/test/front_control_plane_api.test.js:43`, `apps/desk-control-plane/src/features/order-intent/orderIntentDossier.test.tsx:87` | front UX can still improve labels, but contract is ready |
| ProviderCommand | Repository exposes provider command rows; command path remains through execution service | ACK not fill | PARTIAL | `mcp_gpt_desk/src/broker-execution-repository.js:95`, Lot 005/012 tests in full suite | runtime DB has zero provider commands in local proof |
| ProviderLifecycleEvent | Repository/BFF can expose provider events in lifecycle rows | unknown provider state must not crash | PARTIAL | `mcp_gpt_desk/src/broker-execution-repository.js:96`, `mcp_gpt_desk/src/front-control-plane-api.js:1463` | no live provider event runtime sample |
| ReconciliationState | BFF publishes expected/broker/mismatch model when available | mismatch visible, not auto-normalized | FAIT for BFF contract, PARTIAL runtime | `mcp_gpt_desk/src/front-control-plane-api.js:1271`, `apps/desk-control-plane/src/features/order-intent/mapper.ts:79`, `mcp_gpt_desk/test/front_control_plane_api.test.js:618` | real broker mismatch proof remains external |
| CircuitBreakerState | Execution availability and LIVE lock are backend-owned in auth/capabilities | front must not invent provider availability | PARTIAL | runtime `/front-api/v1/capabilities`, `mcp_gpt_desk/src/front-control-plane-auth.js:62` | dedicated circuit-breaker detail contract is not fully exposed in this lot |
| RealtimeEnvelope | SSE emits `eventId`, `schemaVersion`, `sequence`, `correlationId`, `occurredAt`; front tests cover cursor/dedupe | resume/dedupe/gap handling | PARTIAL | runtime `/front-api/v1/events`, `mcp_gpt_desk/test/front_control_plane_api.test.js`, `apps/desk-control-plane` tests | durable DB-backed event replay/resume not proven here |
| StepUpRequirement | Backend session/capabilities publish `requiresStepUp` and read-only blocks | front UX must revalidate before command | PARTIAL | `mcp_gpt_desk/src/front-control-plane-auth.js:80`, runtime `/front-api/v1/views/auth-session` | real operator step-up runtime flow not proven without credentials |

## 6. FRONT_CONTRACT_READY — OrderIntent dossier

Endpoint:

```text
GET /front-api/v1/views/order-detail?orderId=<order-or-portfolio-order-intent-id>
```

Contract properties:

- stable ID: `identity.orderIntentId`
- revision: `humanGate.revision` / `order.expectedVersion`
- null/unknown behavior: unavailable values remain explicit, not fabricated
- authority: `strategy`, `contextGate`, `portfolioArbitration`, `globalRisk`, `targetPosition`
- execution mode: backend-published `SEMI_MANUAL`
- human gate: status + backend-published actions
- provider lifecycle: backend rows only
- reconciliation: expected/broker/mismatch rows

Implementation proof:

- `mcp_gpt_desk/src/front-control-plane-api.js:1249`
- `mcp_gpt_desk/src/front-control-plane-api.js:1279`
- `mcp_gpt_desk/src/front-control-plane-api.js:1298`
- `mcp_gpt_desk/src/front-control-plane-api.js:1328`
- `mcp_gpt_desk/src/front-control-plane-api.js:1381`
- `mcp_gpt_desk/src/front-control-plane-api.js:1463`

Front consumption proof:

- `apps/desk-control-plane/src/domains/front-api/viewModels.ts:2462`
- `apps/desk-control-plane/src/features/order-intent/mapper.ts:8`
- `apps/desk-control-plane/src/features/order-intent/orderIntentDossier.test.tsx:21`

## 7. FRONT_CONTRACT_READY — Commands and allowedActions

Endpoint:

```text
GET /front-api/v1/capabilities
POST /front-api/v1/commands
```

Runtime action catalog after rebuild:

```text
control_plane.verify
research.bootstrap_demo_paper
execution.order_intent.confirm
execution.order_intent.reject
```

All actions were returned with `allowed=false` for anonymous/read-only session. Human Gate commands are catalogued, audited and routed through `store.executeBrokerAction`, not through a direct provider call.

Implementation proof:

- `mcp_gpt_desk/src/front-control-plane-command.js:3`
- `mcp_gpt_desk/src/front-control-plane-command.js:120`
- `mcp_gpt_desk/src/front-control-plane-command.js:151`
- `mcp_gpt_desk/src/front-control-plane-command.js:173`

Tests:

- `mcp_gpt_desk/test/front_control_plane_api.test.js:43`
- `mcp_gpt_desk/test/front_control_plane_api.test.js:434`

Runtime denial proof:

```text
POST /front-api/v1/commands
body: { commandType: "control_plane.verify", idempotencyKey: "lot013-runtime-anon-proof" }
status: 401
body.error: unauthorized
body.message: missing_write_authorization
```

## 8. Positive E2E coverage

Without physical broker execution, the repository now proves:

```text
OrderIntent
→ Human Gate projection
→ backend allowedActions
→ operator command route
→ audited command
→ broker execution service action
→ provider command remains disabled until domain allows it
```

Tests:

```text
node --test mcp_gpt_desk/test/front_control_plane_api.test.js
16 pass / 0 fail

node --test mcp_gpt_desk/test/portfolio_order_intent_human_gate_lifecycle.test.js \
  mcp_gpt_desk/test/portfolio_order_intent_execution_service.test.js \
  mcp_gpt_desk/test/semi_manual_paper_readiness.test.js
23 pass / 0 fail

npm --prefix apps/desk-control-plane test -- orderIntentDossier
1 file / 8 tests pass
```

## 9. Negative E2E coverage

Covered by targeted and full suites:

- anonymous session has no critical action;
- LIVE command is disabled;
- unsupported command is rejected;
- Risk REJECT cannot be force-confirmed;
- expired gate cannot be confirmed;
- stale signal produces no OrderIntent;
- wrong account / implicit LIVE rejected before provider command;
- duplicate confirm remains idempotent;
- quantity/account mutation after Risk rejected;
- ACK is not FILL;
- partial fill is not full FILL;
- provider reject / timeout does not blind-resend;
- reconciliation mismatch remains visible;
- LIMIT theoretical entry does not fill before actual closed-candle touch.

Proof:

- `mcp_gpt_desk/test/portfolio_order_intent_human_gate_lifecycle.test.js`
- `mcp_gpt_desk/test/portfolio_order_intent_execution_service.test.js`
- `mcp_gpt_desk/test/semi_manual_paper_readiness.test.js`
- full MCP suite: `1082 pass / 0 fail`

## 10. Realtime proof

Runtime command:

```text
GET /front-api/v1/events
```

Observed first SSE frame after API rebuild:

```text
id: evt_front_control_plane_heartbeat_31426fe6b2f1d1fd
event: message
data.eventId: evt_front_control_plane_heartbeat_31426fe6b2f1d1fd
data.eventType: desk.snapshot.updated
data.schemaVersion: 1.0.0
data.sequence: 1786693733013
data.correlationId: corr_front_control_plane_1dad4938b1aa
data.payload.source: front-control-plane-bff
```

Status: `PARTIAL` because heartbeat/envelope exists and client tests cover cursor/dedupe paths, but DB-backed replay/resume proof is not closed in this lot.

## 11. PostgreSQL proof

Runtime database:

```text
docker compose exec -T postgres psql -U desk -d desk
```

Tables present:

```text
broker_provider_commands
broker_provider_events
human_execution_gates
portfolio_order_intent_execution_states
portfolio_order_intent_lineage
```

Counts during proof:

```text
broker_provider_commands|0
broker_provider_events|0
human_execution_gates|0
portfolio_order_intent_execution_states|0
portfolio_order_intent_lineage|4
```

Interpretation:

- canonical Portfolio OrderIntent lineage exists locally;
- no provider command/event was fabricated for the proof;
- real broker lifecycle remains `NON PROUVÉ` locally because no provider event rows exist.

## 12. Build/runtime proof

API rebuilt locally:

```text
docker compose up -d --build --no-deps api
```

Result:

```text
tv-automation-preprod-api:latest built
tv-automation-preprod-api-1 recreated
tv-automation-preprod-api-1 healthy
port: 127.0.0.1:8787->8787
```

VNext:

```text
npm run front-vnext:test
35 files / 161 tests pass

npm run front-vnext:build
vite production build OK
```

MCP full suite:

```text
npm --prefix mcp_gpt_desk test
1082 pass / 0 fail
duration_ms 224143.623806
```

## 13. Guards

Passed:

```text
npm run guard:api-compatibility
OK — critical_operations=11, catalog_operations=106
fingerprint=b939c84547cacb1b403fc30a7e2275318e4e08eca7cb6cdf0bedb611e94017a3

npm run guard:architecture
OK — checked_files=391

npm run guard:runtime-safety
OK — direct_clock_usages=130 / budget=130

npm run guard:mcp-slices
OK — actual_tools=117, assigned_tools=117, slice_count=10

npm run guard:sql-migrations
OK — migration_files=54, tables=124

npm run guard:problem-details
OK — schema_version=desk_problem_details_v1, codes=12

npm run guard:security-supply-chain
OK with warnings — mcp_gpt_desk/package-lock.json lacks integrity for 108 dependencies and explicit license for 5 dependencies

npm run guard:front-vnext-data-mode
OK

npm run guard:front-vnext-legacy
OK — checked_files=105

npm run guard:front-architecture
OK
```

Known failing guard:

```text
npm run guard:static-quality
FAILED
- mcp_gpt_desk/src/front-control-plane-api.js has 2299 lines; allowed 600
- packages/desk-domain/src/strategy-dsl-compiler-v1.js has 624 lines; allowed 600
- packages/desk-replay-engine/src/canonical-simulation-engine-v1.js has 881 lines; allowed 600
- oversized functions: 250; allowed 243
- high complexity functions: 650; allowed 592
- duplicate blocks: 72; allowed 50
- possibly dead files: 17; allowed 14
Baseline: docs/engineering/static-quality-baseline.json
```

This is historical debt and is not masked.

## 14. FRONT_AGENT_ACTION_REQUIRED

Front VNext can now consume the backend-published OrderIntent dossier without inventing authority:

1. Render `authority.contextGate`, `authority.portfolioArbitration`, `authority.globalRisk`, `authority.targetPosition`.
2. Render `executionMode` and do not infer `autoExecutionEnabled` from PAPER.
3. Render `humanGate.status` and backend `humanGate.actions`.
4. Build confirm/reject commands only from backend-published `humanGate.actions`.
5. Revalidate session/capabilities/resource revision after step-up.
6. Keep post-risk terms read-only.
7. Keep ACK / PARTIAL_FILL / FILL visually distinct.
8. Show reconciliation mismatch as a blocking visible state.

No visual redesign is required by this lot.

## 15. Status changes

FAIT gained:

- TD2-412 read-only auth no-regression.
- Backend/BFF OrderIntent dossier can project post-risk Portfolio OrderIntent before broker order exists.
- Backend command catalog exposes Human Gate confirm/reject actions.
- BFF Human Gate command route uses backend execution service and does not call provider directly.
- Front VNext mapper consumes backend authority/Human Gate/reconciliation and does not invent them.
- Runtime local API proves read-only fail-closed and SSE envelope availability.

PARTIEL remaining:

- durable SSE resume/gap replay from persisted event store;
- dedicated circuit-breaker contract detail;
- real operator step-up flow with real session cookie/PIN;
- provider lifecycle runtime samples with real command/event rows;
- static-quality debt.

NON PROUVÉ:

- real provider ACK/PARTIAL_FILL/FILL lifecycle in local runtime;
- real broker reconciliation mismatch from provider data;
- VPS runtime parity for this patched release.

BLOQUÉ EXTERNE:

- operator credential/session proof for runtime Human Gate confirm/reject;
- Telegram real channel proof;
- broker/Sim101/provider event proof;
- VPS deployment proof.

## 16. Lot 013 final decision

Lot 013 is `FAIT` for backend/BFF contract readiness and Front VNext consumer certification at repository/local-runtime level.

It is not a production cutover approval. The remaining runtime proofs move to Lot 014 and later cutover/provider lots.
